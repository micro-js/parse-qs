{
  "name": "@f/parse-qs",
  "description": "Query string parser",
  "repository": "git://github.com/micro-js/parse-qs.git",
  "version": "1.0.4",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
